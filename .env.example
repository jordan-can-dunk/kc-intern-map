# Base URL for the backend API.
# Local development (Django running via `manage.py runserver` / Daphne):
VITE_API_BASE_URL=http://localhost:8000

# Production: set this to the deployed backend's URL instead, e.g.
# VITE_API_BASE_URL=https://di-interactive-intern-map-2f78927d0ba7.herokuapp.com
